// Pattern matcher

use cliftinstbuilder::{self, CtonInst, CtonValueDef, CtonInstKind, CtonOpcode};

pub fn generate_patterns(clift_insts: Vec<CtonInst>) {
    /// Fetch the operand of last instruction in the vector
    unimplemented!();
}
